@page "/fetchdata"

@using BlazorTest1.Data
@using BlazorTest1.Data.ApiData
@using BlazorTest1.Services
@*@inject WeatherForecastService ForecastService
@inject OpenWeatherService oService 
@inject StormGlassService sService*@

<h1>Weather forecast</h1>

<p>This component demonstrates fetching data from a service.</p>

@if(root != null){
<table class="table">
    <thead>
        <tr>
            <th>Temp. (C)</th>
            <th>Weather Description</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>@root.current.temp</td>
            <td>@root.current.weather[0].description</td>
        </tr>
    </tbody>
</table>
}
else{
<p><em>Loading...</em></p>
}
    

@*@code {
    OpenWeatherRoot root;
    protected override async Task OnInitializedAsync()
    {
        root = await oService.Get(53, 23, null);
    }
}*@
